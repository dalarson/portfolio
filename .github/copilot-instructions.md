# Copilot Instructions

## Git Policy

- **Never** run `git commit` or `git push`. The developer will handle all commits and pushes manually.
- You may use read-only git commands (e.g., `git status`, `git diff`, `git log`) for context.
